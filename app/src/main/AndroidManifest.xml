<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MovieMood"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Revision">
        <activity
            android:name="com.example.movie.ListFavActivity3"
            android:exported="true" />
        <activity
            android:name="com.example.movie.DetailsActivity"
            android:exported="true" />
        <activity
            android:name="com.example.movie.MainActivity2"
            android:exported="true" />
        <activity
            android:name="com.example.movie.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>